/** Header con el token del alumno, emitido en el onboarding. */
export const STUDENT_TOKEN_HEADER = 'x-ami-student-token';

/** Header con el token de la docente, emitido al canjear el codigo del curso. */
export const TEACHER_TOKEN_HEADER = 'x-ami-teacher-token';

/** Nombres de los esquemas de seguridad tal como aparecen en Swagger. */
export const STUDENT_AUTH = 'student-token';
export const TEACHER_AUTH = 'teacher-token';
